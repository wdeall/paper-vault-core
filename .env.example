# PaperVault 默认配置示例
# 复制为 .env 并填入真实值。.env 不会入 git。
# 注意：API key 建议通过设置页直接保存到 SQLite，不放 .env。

# OpenAI 兼容 API（应用启动时如果数据库为空，会读取这里的值做默认）
VITE_OPENAI_BASE_URL=
VITE_OPENAI_API_KEY=
VITE_OPENAI_MODEL=

# 日志级别
RUST_LOG=info,paper_vault=debug
